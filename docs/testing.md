# 골든 케이스

**`pricing` 코드를 쓰기 전에 이 케이스를 테스트로 먼저 옮긴다.**
정답이 명시되지 않은 계산 로직을 구현하지 않는다.

LLM은 컴파일되지만 **금액이 틀린 코드**를 잘 만든다. "미사용 혜택 0원"처럼
상식과 어긋나는 규칙일수록 학습된 패턴으로 되돌아간다. 그 오류를 발표 당일에 발견하면 늦는다.

---

## 픽스처 (실제 시드와 무관한 고정 데이터)

**요금제**

| ID | 이름 | 기본료 | 제휴 혜택 |
|---|---|---|---|
| P1 | 넷플릭스형 | 55,000 | 넷플릭스 스탠다드 `FREE` |
| P2 | 웨이브형 | 45,000 | 웨이브 스탠다드 `FREE` |
| P3 | 택1형 | 50,000 | 넷플·티빙·웨이브 중 택1 `FREE` |
| P4 | 정액할인형 | 48,000 | 넷플릭스 `FIXED_DISCOUNT` 4,000 |
| P5 | 절사검증형 | 33,333 | 없음 |

**티어**: T2 넷플 스탠다드 13,500 · T6 티빙 광고형 5,500 · T8 티빙 스탠다드 13,500 · T12 웨이브 스탠다드 10,900
**번들**: B4 티빙×웨이브 더블 스탠다드 15,000 (T8+T12)

---

## G-01. 미사용 혜택 0원 — 순위 역전 ★최우선

**검증**: `CostCalculatorGoldenTest` · `RecommendationApiTest`

| | 원하는 서비스 | P1 | P2 | 정답 |
|---|---|---|---|---|
| a | {T2 넷플릭스} | 55,000 + **0** = 55,000 | 45,000 + 13,500 = 58,500 | **P1** |
| b | {T12 웨이브} | 55,000 + 10,900 = 65,900 | 45,000 + **0** = 45,000 | **P2** |

약정 없음 기준. 요금제 쌍은 그대로인데 원하는 서비스만 바꾸면 **순위가 뒤집혀야 한다.**

**G-01-b의 `CostBreakdown`에 "넷플릭스" 항목이 한 줄이라도 있으면 실패로 처리한다.**

## G-02. 선택약정 25%

**검증**: `CostCalculatorGoldenTest`

| | 기본료 | 약정 | 정답 |
|---|---|---|---|
| a | 55,000 | `SELECTIVE_25` | 41,250 |
| b | 55,000 | `NONE` | 55,000 |
| c | 33,333 | `SELECTIVE_25` | **24,999** |

c: `33,333 × 0.75 = 24,999.75` → **내림** → 24,999. 반올림(25,000)이면 실패.

## G-03. 할인 적용 순서

**검증**: `CostCalculatorGoldenTest`

기본료 55,000, 요금제 약정할인 4,800, 선택약정 25%.

```
1) 약정할인      55,000 - 4,800 = 50,200        (priority 100)
2) 선택약정 25%  floor(50,200 × 0.75) = 37,650  (priority 200)
```

정답 **37,650**. 순서를 바꾸면 36,450이 나온다. 1,200원 차이는 눈으로 안 보인다.

## G-04. 번들 최저경로

**검증**: `CostCalculatorGoldenTest` · `CalculatorApiTest`

원하는 서비스 {T8, T12}, 요금제 P5(제휴 없음).

| 경로 | 구독료 |
|---|---|
| 개별 | 13,500 + 10,900 = 24,400 |
| 번들 B4 | **15,000** |

정답 총비용 **48,333**. `CostBreakdown`에 번들 한 줄로 나와야 하며,
티빙·웨이브가 각각 별도 줄이면 실패.

## G-05. 택1(exclusive) 최적 선택

**검증**: `CostCalculatorGoldenTest`

P3(택1 무료), 원하는 서비스 {T2 넷플 13,500, T6 티빙 5,500}.

| 선택 | 구독료 합 |
|---|---|
| 넷플릭스 무료 | 0 + 5,500 = **5,500** ✅ |
| 티빙 무료 | 13,500 + 0 = 13,500 ❌ |

정답 총비용 **55,500**. **절감액이 큰 쪽**을 고르는지 검증. ID 순서로 고르면 실패.

## G-06. 정액 할인 하한

**검증**: `CostCalculatorGoldenTest`

P4(넷플 `FIXED_DISCOUNT` 4,000), {T2} → 48,000 + (13,500 - 4,000) = **57,500**

추가: 할인액이 정가보다 크면 결과는 **0**. 음수가 나오면 실패.

## G-07. baseline과 절감액

**검증**: `CostCalculatorGoldenTest`

G-01-a 조건: baseline **68,500** (55,000+13,500) · effective **55,000** ·
monthlySavings **13,500** · annualSavings **162,000**

baseline은 **할인 하나도 없을 때의 정가 합**이다. 다른 요금제와 비교한 값이 아니다.

## G-08. missingInputs

**검증**: `CostCalculatorGoldenTest`

`hasFamilyBundle` 없음 → `accuracy = PARTIAL`, `missingInputs`에 해당 항목 존재.
선택 필드가 모두 차면 `FULL`, `missingInputs = []`.

## G-09. 중복 결제 탐지 (P1)

**검증**: `DuplicateDetectorGoldenTest` · `DetectionServiceTest`

| | 상황 | 정답 |
|---|---|---|
| a | P2(웨이브 무료) + 웨이브 10,900 결제 중 | `BENEFIT_OVERLAP`, 10,900 |
| b | 넷플 스탠다드 + 프리미엄 동시 결제 | `TIER_DUPLICATE`, 13,500 |
| c | 티빙 + 웨이브 개별 결제 | `BUNDLE_OVERLAP`, 9,400 |
| d | P1(넷플 무료) + **웨이브** 결제 중 | **탐지 없음** |
| e | 요금제가 그 서비스를 **`BUNDLE_INCLUDED`(등급 미상)** 로 포함 + 그 서비스 결제 중 | `BENEFIT_OVERLAP`, 내는 금액 전액, 출처 **`ESTIMATED`** |
| f | **`FIXED_DISCOUNT`**(등급 지정) + 정가로 결제 중 | `BENEFIT_OVERLAP`, `내는금액 − 혜택적용가`, 출처 **`DERIVED`** |
| g | f 인데 이미 혜택가 이하로 내는 중 | **탐지 없음**. 낭비가 0 이하면 낭비가 아니다 |

d가 중요하다. 혜택이 있어도 **다른 서비스**를 결제 중이면 중복이 아니다.

**e 를 추가한 이유(2026-09-17).** 운영에 실제로 이런 회원이 있었고 탐지가 침묵했다 —
현재 요금제가 KT `초이스 더블 유튜브 프리미엄+넷플릭스`(이름부터 넷플릭스 포함)인데
넷플릭스 광고형 스탠다드를 7,000원 따로 결제 중이었다. 그 혜택이 `BUNDLE_INCLUDED`·등급 미상이라
`benefitType == FREE` 검사에서 탈락했다. 운영 혜택 73행 중 **`BUNDLE_INCLUDED` 가 51행(70%)** 이므로
이 패턴이 통째로 새고 있었다.

**금액을 단정하지 않는다.** 등급을 모르면 얼마가 낭비인지 확정할 수 없다 — 전액일 수도, 상위 등급
차액만일 수도 있다. 그래서 e 의 출처는 `ESTIMATED` 이고 **표시 전용**이다(D-17). 추천 금액 계산에는
들어가지 않는다. 확정할 수 없다고 침묵하는 것보다 "확인해 보세요"가 사용자에게 낫다.

## G-10. 가맹점명 정규화

**검증**: `MerchantNormalizerGoldenTest` · `PaymentImportServiceTest`

| 입력 | 정답 `service_id` |
|---|---|
| `NETFLIX.COM` | 1 |
| `넷플릭스` | 1 |
| `GOOGLE *YOUTUBEPREMIUM` | 6 |
| `콘텐츠웨이브(주)` | 4 |
| `배달의민족` | **null** |

마지막이 핵심이다. 모르는 가맹점은 **null로 두고 사용자에게 묻는다.**
유사도로 억지 매핑하면 실패.

## G-11. 회수기간 (P2)

**검증**: `SwitchTimingGoldenTest`

| | 전환비용 | 월절감 | 약정잔여 | 회수 | 정답 |
|---|---|---|---|---|---|
| a | 60,000 | 17,700 | 10개월 | 4 | `SWITCH_NOW` |
| b | 60,000 | 17,700 | 2개월 | 4 | `WAIT_UNTIL_EXPIRY` |
| c | 60,000 | 0 | 10개월 | — | `NO_BENEFIT` |
| d | 0 | 17,700 | 10개월 | 0 | `SWITCH_NOW` |
| e | `Long.MAX_VALUE` | `Long.MAX_VALUE` | 10개월 | 1 | `SWITCH_NOW` (덧셈 오버플로 방지) |
| f | `Integer.MAX_VALUE + 1` | 1 | 10개월 | 범위 초과 | 계산 거부 (음수로 축소 변환 금지) |
| g | 60,000 | 17,700 | **0개월** | 4 | `SWITCH_NOW` (약정이 이미 끝났으면 기다릴 대상이 없다) |

`ceil(60,000/17,700) = ceil(3.389) = 4`. 내림으로 3이면 실패. c는 0 나눗셈 방어.
g는 약정 잔여 0이면 회수개월과 무관하게 `SWITCH_NOW` — `회수 < 잔여` 비교만 쓰면 `4 < 0` 이 거짓이라 "끝난 약정을 기다리라"는 결론이 나온다.
HTTP에서는 음수 전환비용·음수 약정잔여 및 회수 개월의 응답 정수 범위 초과를 400으로 반환한다.

---

## G-12. 카탈로그 결손은 막지 않는다 (D-17)

**검증**: `RecommendationApiTest`

CSV에 없는 것을 물어도 **오류를 내지 않는다.** 아는 것으로 계산하고, 모르는 것은 안내하고 기록한다.
절대 원칙 5-① — "모르면 막히는 화면을 만들지 않는다".

| | 입력 | 정답 |
|---|---|---|
| a | `wantedServiceIds=[1, 99]` (99는 없음) | **200**. 1로만 계산. `missingInputs`에 `wantedServiceIds` 항목, `accuracy=PARTIAL`. `catalog_candidate`에 `(SUBSCRIPTION_TIER, "serviceId:99", REQUESTED)` 1행 |
| b | `wantedServiceIds=[99]` (전부 없음) | **200**. 원하는 서비스 없이(혜택 0원) 기본료로 계산하며 **결과는 비지 않는다**. 99는 기록 |
| c | 조건 만족 요금제 0건 (`monthlyDataGb=200`) | **200**. `results=[]` + 안내. `(MOBILE_PLAN, "dataMb>=204800,network=ANY", REQUESTED)` 기록 |
| d | c를 한 번 더 요청 | 행이 늘지 않고 `requested_cnt=2` |
| e | `catalog_candidate`가 상한(10,000행)에 도달 | 기록하지 않는다. **요청은 그대로 200** — 공개 엔드포인트라 무한 증식을 막는다 |

이전 동작은 a·b가 **400** `YGB-REQ-001`, c가 **422** `YGB-CAL-001` 이었다. 둘 다 사용자를 막았다.
기록 실패(DB 오류)는 추천을 깨뜨리지 않는다 — fail-soft, 로그만 남기고 200을 유지한다.

---

## G-13. 메일 없이 가입 (D-20 · D-21) — **D-34 로 폐기 (2026-09-17)**

**검증**: **없음 — 폐기.** 옛 DirectSignupTest 는 삭제됐다(닉네임 부분만 `NicknameTest` 로 남았다)

> 가입 경로가 Google 하나가 되면서 자체 가입이 사라졌다. 아래 케이스는 **더 이상 검증하지 않는다** —
> DirectSignupTest 도 지웠다(닉네임 부분만 `NicknameTest` 로 옮겼다). 기록으로만 남긴다. 현재 가입 규칙은 G-17(아래)이다.

`{name, email, password, nickname}`으로 바로 가입한다. **가입 형태는 이것 하나뿐이다** —
메일 토큰 흐름은 2026-09-16 에 코드에서 제거했다.
**이메일 소유는 확인하지 않는다** — `email_verified`는 `FALSE`로 남고 로그인은 그 값을 보지 않는다.

| | 입력 | 정답 |
|---|---|---|
| a | 네 필드 정상 | **200** 가입 + 쿠키 발급. `email_verified=FALSE` |
| b | a 직후 같은 이메일로 다시 | **409** `YGB-AUTH-DUP-EMAIL` |
| c | a 직후 같은 닉네임·다른 이메일 | **409** `YGB-AUTH-DUP-NICK` |
| d | a로 가입한 계정으로 로그인 | **200** (미확인이어도 통과 — 확인할 방법이 없다) |
| e | 비밀번호 8자 미만 | **400** `YGB-REQ-001` |
| e2 | 숫자만(`12345678`) 또는 문자만(`abcdefgh`) | **400** — 문자와 숫자를 섞어야 한다 (사용자 결정 2026-09-16) |
| e3 | `abcd1234`(8자·혼합) | **200**. 72바이트 초과는 여전히 400(bcrypt 입력 한계) |
| f | `{token, password}` (옛 메일 토큰 형태) | **400** — `name`·`email` 누락. 토큰 가입은 없어졌다 |

b·c를 구분하는 이유: "이미 가입된 이메일"과 "이미 쓰는 닉네임"은 사용자가 할 일이 다르다.
**남는 위험**: 이메일 소유를 아무도 확인하지 않으므로 타인의 주소로 가입할 수 있다(`auth-security.md`).

---

## G-14. 닉네임 자동 발급·변경 (D-22 · D-34 로 a 폐기)

가입에 닉네임 입력란이 없다. 서버가 `이름(또는 이메일 앞부분) + 숫자 5자`로 만들어 준다.
**검증**: `NicknameTest`(2026-09-17 신설, 옛 DirectSignupTest 의 살아남은 절반. 그 파일은 삭제됐다).

| | 입력 | 정답 |
|---|---|---|
| ~~a~~ | ~~직접 가입 시 `nickname` 생략~~ | **D-34 로 폐기** — 직접 가입이 없다 |
| b | Google 로그인 (이름 없음) | 닉네임 = **이메일 로컬파트** + 5자리 숫자 |
| c | 같은 로컬파트를 쓰는 다른 Google 계정 | 닉네임이 겹치지 않는다. 재시도해도 안 되면 500이 아니라 더 긴 값으로 성공 |
| d | `POST /me/nickname` 로 변경 | 200. `GET /me` 에 반영 |
| e | 남이 쓰는 닉네임으로 변경 | **409** `YGB-AUTH-DUP-NICK` |
| f | 자기 닉네임을 그대로(대소문자만 다르게) | 200 (자기 자신은 충돌이 아니다) |

이름에서 닉네임에 못 쓸 문자를 빼고, 남는 게 없으면 `user` 를 쓴다.

## G-15. 복구 코드로 비밀번호 재설정 (D-22) — **D-34 로 폐기 (2026-09-17)**

**검증**: **없음 — 폐기.** 보관 사유는 아래 본문

> 우리가 회원 비밀번호를 보관하지 않으므로 재설정할 것이 없다. 아래 케이스는 더 이상 검증하지 않는다.
> **G-15h(복구 시 세션 폐기)만은 교훈으로 남긴다** — 명세는 맞았는데 코드가 지키지 않았고 강제하는 테스트가
> 없었다. 같은 실수를 막으려면 "문서에 적힌 보안 속성에는 반드시 테스트가 붙는다"를 지켜야 한다.

메일이 없으므로 **복구 코드**가 유일한 자기복구 수단이다. 가입 응답에 **한 번만** 나오고 서버는 해시만 갖는다.

| | 입력 | 정답 |
|---|---|---|
| a | 직접 가입 | 응답에 `recoveryCode` 1회 포함. DB 에는 **평문이 없다** |
| b | `{email, recoveryCode, newPassword}` 로 재설정 | 200 + **새 복구 코드** 발급. 기존 세션 전부 폐기 |
| c | b 직후 같은 코드로 다시 | **401** — 코드는 1회용이다 |
| d | 틀린 코드 | **401**. 존재하지 않는 이메일도 같은 응답(계정 존재를 알려주지 않는다) |
| e | 재설정한 새 비밀번호로 로그인 | 200 |
| f | 재설정 전 비밀번호로 로그인 | **401** |
| g | 같은 이메일로 코드 시도 10회 초과 | **429** (무차별 대입 방어) |
| h | 복구 **전에 열려 있던** 로그인 세션 | 전부 **401**. 남는 세션은 복구한 본인의 새 세션 하나뿐 |

b가 새 코드를 주는 이유: 한 번 쓰면 사라지므로 다음 복구 수단이 없어진다.
h를 따로 적는 이유: b의 "기존 세션 폐기"는 **2026-09-16까지 지켜지지 않았다.** `credential_version` 은
발급 시점에만 검사되는데(`AuthTokens.authenticate` 는 `auth_session` 만 본다) 코드가 그 값만 올리고
세션 행을 지우지 않아, 되찾아도 침입자 세션이 살아 있었다. 명세는 맞았고 이를 강제하는 테스트가 없었다.
`GET /me` 는 복구 코드를 **절대 돌려주지 않는다** — 로그인한 사람에게도 보여줄 이유가 없다.

---

## G-16. 캘린더 약정 만료일 직접 입력 (D-22 후속)

**검증**: **없음 — 화면 로직이다**(`front/src/calendar.js`). BE 계약이 아니라 프론트 테스트 대상이다

라이트 모드에는 약정 입력이 없다. 캘린더에서 직접 받아 같은 규칙으로 일정을 잡는다.

| | 입력 | 정답 |
|---|---|---|
| a | 만료일 입력란 비움 | 오늘 기준 일정 + "약정 정보가 없어 오늘 기준" 안내 |
| b | 미래 날짜 입력 | **즉시** 그 날 기준으로 재배치(−14/−7/−3/0/+30) + 안내 문구 변경 + 그 달로 이동 |
| c | 과거 날짜 입력 | 오늘 기준 유지 + "이미 지나서" 안내 |
| d | 디테일 모드에서 이미 입력한 경우 | 그 값이 입력란에 채워져 있다 |

입력값은 `yogobi:input` 의 `contract` 에 저장해 결과 화면과 같은 값을 쓴다(절대 원칙 5-⑤).

---

## G-17. 해외 결제 구독 — 원화는 표시만, 계산은 사용자 금액 (사용자 결정 2026-09-16)

**검증**: `ForeignCurrencyCatalogTest` · `ExchangeRatesTest`

ChatGPT·Claude 처럼 **USD 로 파는 구독**을 카탈로그에 넣되 원화 금액을 지어내지 않는다.
환율은 하루 1회 배치로 받아 `fx_rate` 에 남기고, 화면에는 `$20 · 약 27,183원(추정)` 으로 **표시만** 한다.

| | 입력 | 정답 |
|---|---|---|
| a | `GET /catalog/services` 의 ChatGPT Plus | `price=20` · `currency="USD"` · `krwEstimate`= 환율 환산값 · `krwRateDate`= 기준일 |
| b | 같은 응답의 넷플릭스 광고형 스탠다드 | `price=7000` · `currency="KRW"` · `krwEstimate=null` · `krwRateDate=null` |
| c | `fx_rate` 에 USD 행이 없을 때 a | `krwEstimate=null` (0원으로 적지 않는다) |
| d | 추천 요청의 `wantedServiceIds` 에 ChatGPT | **200**. 결과는 나오고 `missingInputs` 에 "해외 결제라 … 계산에서 뺐어요" |
| e | d 의 ChatGPT | `catalog_candidate` 에 **기록하지 않는다** (결손이 아니라 사용자 확인 대상) |
| f | 계산기 `tierIds` 에 USD 등급 | **200**. 그 등급만 빼고 계산하고 같은 안내를 싣는다 |
| g | 계산기 `tierIds` 에 없는 ID | **400** (기존 동작 유지 — 잘못된 요청) |
| h | 환율 API 호출 실패 | 이전 값 유지. 화면은 계속 이전 기준일로 표시한다 |
| i | 환산 반올림 | **내림(floor)**. $20 × 1359.15 = 27,183원 |
| j | 표기가가 **세금 별도**인 등급(클로드 Pro) | `taxIncluded=false`. `krwEstimate` 는 부가세 10%를 더한 값 — $20 × 1.1 × 1359.15 = **29,901원** |
| k | 원화 등급 | `taxIncluded=true`. 표시가가 총액이므로 10%를 더하지 않는다 |

j 가 필요한 이유: 해외 사업자의 표기가는 세금 별도라 실제 결제액이 10% 더 크다(전자적 용역 공급 부가세).
표기가에 1.1을 곱해 **저장하면 출처가 사라지므로** 저장은 표기가 그대로 두고 `tax_included` 로 구분한다.

d·f 가 200 인 이유: 모르면 막지 않는다(절대 원칙 5-①). 금액을 지어내지 않으면서 결과는 보여준다.
**계산에 들어가는 유일한 경로**는 사용자가 마이페이지 > 내 구독에 넣은 실제 결제액(`USER_PROVIDED`)이다.

---

## G-18. 가입할 수 없는 요금제는 추천하지 않는다 (연령·자격 제한)

**검증**: `AgeLimitCandidateTest`

`mobile_plan.age_limit` 은 CSV·DB 에 채워져 있으나 **조회가 쓰지 않아** 40세 사용자에게 키즈 요금제가,
성인에게 청년 요금제가 후보로 들어갔다. 가입 자격이 없는 요금제를 1등으로 보여주면 사용자는
가입하러 갔다가 실패한다 — 절대 원칙 1("쓸 수 없는 혜택은 0원")과 같은 논리다.

시드 실측 분포(1,706종): `ALL` 1,504 · `다이렉트` 44 · 나머지 158(키즈·청년·시니어·복지·외국인·군인·태블릿).

| 케이스 | age_limit | 기대 |
|---|---|---|
| a | `ALL` | 후보에 **포함** |
| b | `다이렉트` | 후보에 **포함** — 온라인 가입 채널일 뿐 자격 제한이 아니다. 요고 13종이 여기 속한다 |
| c | `NULL` | 후보에 **포함** — 수집되지 않은 값을 제한으로 해석하지 않는다 |
| d | `청년 만 19~34세` | 후보에서 **제외** |
| e | `키즈 만 12세 이하` | 후보에서 **제외** |
| f | `태블릿/웨어러블` | 후보에서 **제외** — 휴대폰 요금제가 아니다 |
| g | 제외된 것이 1건 이상 | `missingInputs` 에 `ageLimit` 안내 1건. 결과는 **200**, 남은 후보로 계산은 끝까지 간다 |
| h | 제외된 것이 0건 | `ageLimit` 안내를 넣지 않는다 — 없는 선택지를 안내하면 소음이다 |

**나이를 입력받아 판정하지 않는 이유**: 요청 스키마에 나이를 더하는 것은 `architecture.md` §3
계약 변경이라 사람 승인이 필요하다(별도 제안). 승인 전까지는 제외하고 안내만 한다 —
`청년`(18종)처럼 구간 표기가 없는 값은 나이를 받아도 판정할 수 없으므로, 업계 관행으로
"만 34세 이하"를 **지어내지 않는다**.

g 가 200 인 이유: 모르면 막지 않는다(절대 원칙 5-①). 자격을 확인할 방법이 없어도 결과는 보여준다.

---

## G-19. 스마트초이스가 아는 요금제 중 우리에게 없는 것을 결손으로 남긴다 (D-31)

**검증**: `SmartChoiceMissingPlanTest`

공식 API 는 `AGE = 20` 으로 스윕하므로 청년 요금제를 이미 받아온다 — KT Y덤 11종, SKT 라이트(청년),
LGU+ 데이터플랜(유쓰+). 그런데 스냅샷에만 쌓이고 카탈로그에는 반영되지 않아, 실측 결과
**Y덤 11종이 전부 빠져 있었다**(원본 `KT 베이직21GB` 는 있는데 변형 `KT 베이직21GB Y덤` 이 없다).

스냅샷은 여전히 카탈로그 원본이 아니다(D-20 유지). 여기서 만드는 것은 **수집 우선순위 목록**이고,
카탈로그 진입은 사람이 CSV 에 반영할 때뿐이다(D-18 절차 그대로).

| 케이스 | 상황 | 기대 |
|---|---|---|
| a | 스냅샷에 있고 카탈로그에 없다 | `catalog_candidate` 에 `MOBILE_PLAN` · `"<통신사> <요금제명>"` 로 1행 |
| b | 스냅샷에 있고 카탈로그에도 있다 | 기록하지 않는다 |
| c | 표기만 다르다 (`KT 베이직21GB` / `kt 베이직 21GB`) | 같은 것으로 보고 기록하지 않는다 — 공백·대소문자 무시 |
| d | 배치가 여러 번 돈다 | 행이 늘지 않고 **`requested_cnt` 도 오르지 않는다** |
| e | 상한(`MAX_ROWS`)에 닿았다 | 기록만 멈추고 스윕은 끝까지 간다 |
| f | 기록이 실패한다 | 스냅샷 저장은 유지된다 (fail-soft) |

**d 가 핵심이다.** `requested_cnt` 는 "사용자가 몇 번 찾았나"이고 그것이 수집 우선순위다.
하루 3회 배치가 이 값을 올리면 아무도 찾지 않은 요금제가 우선순위 1위가 된다 — 배치는 `last_requested_at` 만 갱신한다.

---

## 기준

| 대상 | 커버리지 | 강제 |
|---|---|---|
| `pricing/**` | 라인 90%, **분기 100%** | ✅ `./gradlew check` |
| `detection/**` | 라인 90%, **분기 100%** | ✅ `./gradlew check` |
| 그 외 | 강제 안 함 | — |

**2026-09-17 전까지 이 표는 재지 않는 기준이었다.** 도구가 없어 한 번도 검증되지 않았고,
실제로 `pricing` 분기는 87.5%(9개 미달)였다. JaCoCo 를 붙여 측정하고 `jacocoTestCoverageVerification`
으로 빌드가 강제하게 했다. 숫자의 출처는 이 표이며 `build.gradle` 은 그 계약을 지키는 장치일 뿐이다.
가드가 실제로 무는지도 확인했다 — 기준을 실측보다 높게 올리면 정확한 메시지와 함께 빌드가 실패한다.

**`detection` 기준을 올렸다(2026-09-17).** 라인 80%만 요구하던 탓에 분기는 73.7%(10개 미달)였다.
탐지는 "당신은 돈을 버리고 있다"고 말하는 기능이라 **틀린 경고가 틀린 절감액보다 나쁘다** —
사용자가 멀쩡한 구독을 해지한다. `pricing` 과 같은 기준(라인 90% · 분기 100%)으로 맞췄다.

현재값(2026-09-17): `pricing`·`pricing/domain`·`pricing/rule`·`detection`·`detection/domain`
**다섯 패키지 모두 라인·분기 100%**.

새 `DiscountRule`은 골든 케이스 없이 머지하지 않는다.
**케이스 명세 → 테스트 → 구현** 순서다.

## 통합 테스트

| 대상 | 방식 |
|---|---|
| DB | Testcontainers PostgreSQL |
| AI 서버 | WireMock 스텁, 실제 호출 없음 |
| 스마트초이스 API | 스텁. 실패해도 추천은 정상 동작 (fail-soft) |

### 시드 무결성 (팀원 CSV 병합 시 필수)

```sql
SELECT * FROM mobile_plan WHERE base_price IS NULL OR base_price <= 0;
SELECT * FROM mobile_plan WHERE source_url IS NULL OR source_url = '';
SELECT carrier_id, name, COUNT(*) FROM mobile_plan GROUP BY 1,2 HAVING COUNT(*) > 1;
SELECT * FROM plan_benefit b LEFT JOIN mobile_plan p ON b.mobile_plan_id=p.id WHERE p.id IS NULL;
```

---

## G-20. 가입·로그인은 Google 하나뿐 (D-34)

**검증**: `AuthSecurityTest`

사용자가 보는 것은 버튼 하나다. Google `sub` 가 있으면 로그인, 없으면 가입 — 서버가 나눈다.
검증은 `AuthSecurityTest`(실제 OIDC 스텁으로 state·nonce·PKCE·서명까지 통과시킨다).

| | 입력 | 정답 |
|---|---|---|
| a | 처음 보는 `sub` 로 콜백 | 가입 + 인증 쿠키. `email_verified=TRUE`(Google 이 확인해 준 값) |
| b | 같은 `sub` 로 다시 콜백 | 같은 회원으로 로그인. 이메일이 바뀌어 있어도 **`sub` 가 신원이다** |
| c | 같은 이메일 + **다른 `sub`** | 자동 병합하지 않는다 → `#auth=account-conflict`, 회원 생성 없음 |
| d | `POST /api/v1/auth/signup` · `/login` · `/password/recover` · `/google/link` · `/password` | **403** — 경로 자체가 없다 |
| e | `GET /me` 의 `localLogin` | **항상 false**. 우리가 보관하는 비밀번호가 없다 |
| f | `/oauth2/authorization/google` 를 IP 당 15분에 40회 초과 | **429**. `X-Forwarded-For` 를 바꿔도 같은 발신지로 센다 |

d 를 적는 이유: 지워진 경로가 조용히 되살아나면 아무도 모른다. **없어졌다는 것 자체가 검증 대상이다.**

## G-21. 결과 리포트는 로그인 후에만 (D-36) — **미구현**

**검증**: **없음 — 미구현**. 화면 반영 시 프론트 테스트가 맡는다

| | 입력 | 정답 |
|---|---|---|
| a | 비로그인으로 결과 화면 진입 | 리포트 대신 "Google 로 계속하기". **금액은 한 줄도 노출되지 않는다.** 입력값은 유지된다 |
| a2 | 입력 단계에서 예상 절감액 노출 | **없다.** 티저 없이 결과 화면에서 한 번에 연다(사용자 결정 2026-09-17) |
| a3 | 입력 전 "로그인이 필요하다" 고지 | **하지 않는다.** 사전 고지는 이탈이 높게 나오면 꺼낼 카드다 |
| b | a 에서 로그인 완료 | 같은 입력으로 **다시 묻지 않고** 결과가 그려진다 |
| c | 비로그인으로 `POST /api/v1/recommendations` 직접 호출 | **200** — 화면 게이트이지 API 권한이 아니다(의도된 우회 가능) |
| d | 게이트 도달·로그인 완료·게이트 이탈 | 백오피스 지표에 각각 남는다(D-32). 이게 없으면 CBT 판단 근거가 없다 |

c 를 적는 이유: 게이트가 우회 가능하다는 것은 **결함이 아니라 선택**이다. 나중에 막기로 하면 그때 계약을 바꾼다.

## G-22. 카탈로그 원본 경로가 없을 때 (2026-09-17)

**검증**: `CatalogAdminDisabledTest`

**이것이 운영의 정상 상태다(D-37).** 레포 CSV 가 원본이라 편집 경로를 일부러 열지 않는다 —
여기서 503 이 나는 것은 설정 누락이 아니라 결정의 이행이다.
운영에는 `yogobi.catalog.combined-csv` 가 설정돼 있지 않다. 그 설정 그대로 도는 테스트가
`CatalogAdminDisabledTest` 다 — **기존 카탈로그 테스트 셋은 전부 경로를 넣고 돌아서** 이 조건을 놓쳤다.

| | 입력 | 정답 |
|---|---|---|
| a | `GET /api/v1/admin/catalog`, `GET /{dataset}` | **200.** 읽기는 클래스패스 합본 CSV 로 폴백한다 |
| b | `POST`·`PATCH`·`DELETE /api/v1/admin/catalog/…` | **503** `YGB-CAT-503` (500 아님) |
| c | b 이후 `catalog_change_request` 행 수 | **0.** 적용할 수 없는 제안을 쌓아 두지 않는다 |
| d | PENDING 제안에 `POST /requests/{id}/approve` | **503**, 그리고 제안은 **PENDING 그대로**·`decided_by` 는 NULL |

d 가 핵심이다. 가드가 `claim()` 뒤에 있으면 설정 문제로 **남의 제안이 FAILED 로 닫히고**,
경로를 채워도 그 제안은 되살릴 수 없다. 서버 설정 때문에 사용자 입력을 버리면 안 된다.

## G-23. 로그인 게이트 퍼널 집계 (D-36)

**검증**: `FunnelCounterTest` · `AuthSecurityTest`(실제 OIDC 왕복)

"이탈율이 높으면 비회원에게 연다"는 기준은 숫자가 있어야 작동한다. 검증은 `FunnelCounterTest`
(+ 실제 OIDC 왕복은 `AuthSecurityTest`).

| | 입력 | 정답 |
|---|---|---|
| a | 비회원이 `POST /recommendations` 로 결과를 받음 | `GATE_SHOWN` +1 (게이트를 만났다) |
| b | 회원이 같은 요청 | `REPORT_SHOWN` +1 (리포트를 봤다). `GATE_SHOWN` 은 그대로 |
| c | 같은 날 3회 | 값은 3, **행은 1개**. 이벤트마다 쌓지 않고 날짜×종류로 합친다 |
| d | `funnel_daily` 가 없는 상태에서 추천 | **200.** 집계가 죽어도 사용자는 결과를 받는다 |
| e | 백오피스 대시보드 | 게이트·리포트·로그인은 실측, 이탈은 `gateDropEstimate`(뺄셈)로 **이름이 다르다** |
| f | 실제 Google 로그인 성공 | `MEMBER_LOGIN` +1. 실제 state·nonce·PKCE·서명을 통과한 지점에서만 센다 |

d 가 중요하다. 지표 때문에 기능이 멈추면 안 된다 — 집계는 별도 트랜잭션이고 실패는 로그만 남긴다.
e 도 중요하다. 이탈은 유일하게 측정값이 아니므로 화면에서 실측과 섞이면 나중에 판단 근거가 흐려진다.
다만 **대시보드가 읽기에 실패할 때도 로그를 남긴다** — 조용히 삼키면 쿼리 버그가 "지표 없음"으로 위장한다
(실제로 개발 중 `day` 별칭 모호성으로 한 번 그렇게 위장했다).

## G-24. 카탈로그 원본이 갈라졌을 때 (D-24 단일 원본의 구멍)

**검증**: `CombinedCatalogStoreTest`

외부 경로(`yogobi.catalog.combined-csv`)를 쓰면 원본이 둘이 된다 — 외부 파일과 jar 내장 CSV.
**갈라지면 외부 파일이 이기고 레포 CSV 는 조용히 무시된다.** 어느 쪽이 이겨야 하는지는 제품 결정이라
코드가 고르지 않는다. 대신 갈라졌다는 사실을 드러낸다. 검증은 `CombinedCatalogStoreTest`.

| | 입력 | 정답 |
|---|---|---|
| a | 외부 파일 == 내장 CSV | `diverged()` **false** |
| b | 외부 파일이 한 글자라도 다름 | `diverged()` **true** |
| c | 기동 시 b 상태 | 경고 로그 — "외부 파일이 이기고 레포 CSV 는 무시됩니다" |
| d | 백오피스 대시보드 | `catalog.sourceDiverged` 로 노출 |

**왜 고르지 않았나:** 외부가 이기면 레포 CSV 수정 배포가 먹지 않고, 내장이 이기면 운영자 편집이
배포마다 사라진다. 둘 다 사용자에게 치명적이고 어느 쪽이 맞는지는 운영 방식이 정한다.
**D-37 로 운영에서는 이 조건이 발생하지 않는다** — 외부 경로를 쓰지 않기로 정했다.
그래도 검사를 남기는 이유: 로컬에서 경로를 켠 채 작업하다 레포 CSV 와 어긋나는 일이 실제로 생기고,
그때 조용하면 원인을 못 찾는다.

## G-25. 골든 케이스가 지나친 pricing 분기 (2026-09-17)

**검증**: `PricingEdgeGoldenTest`

`pricing` 분기 100% 기준을 채우며 드러난 경로들이다. **전부 금액이 달라지는 분기**이고,
커버리지 숫자를 올리려고 만든 케이스가 아니다. 검증은 `PricingEdgeGoldenTest`.

| | 입력 | 정답 |
|---|---|---|
| a | 번들에 묶인 등급 중 일부만 원함 | 번들을 **쓰지 않는다**. 원하지 않는 것을 끼워 총액을 낮추면 절대 원칙 1 위반 |
| b | 번들가 ≥ 개별 합계 | 쓰지 않는다. **같을 때도** 쓰지 않는다 — 이득 없이 해지 자유만 잃는다 |
| c | 요금제 혜택으로 이미 무료인 등급이 번들에 포함 | 비교는 **혜택 적용 개별가**로 한다. 정가로 비교하면 번들이 유리해 보인다 |
| d | 혜택 유형 4종 | FREE→0 · FIXED_DISCOUNT→정액 차감 · RATE_DISCOUNT→BigDecimal 내림 · BUNDLE_INCLUDED→정가 유지 |
| e | 정액 할인 > 정가 | **0**. 음수로 내려가지 않는다 |
| f | `tierId` 가 null | 그 서비스 **모든 등급**에 걸린다. 지정하면 그 등급에만 |
| g | `Money.floorToZero` | 음수 → 0, 0 → 0, 양수 → 그대로. 절감액을 음수로 적지 않는다 |
| h | 음수 전환비용·음수 약정잔여 | **거부**(IllegalArgumentException). 조용히 0 으로 고치면 금액이 조용히 틀어진다. 0 은 정상 |

b 와 h 가 핵심이다. b 는 "같을 때"라는 경계를 명시하고, h 는 **잘못된 입력을 삼키지 않는다**는 규칙이다 —
금액을 다루는 코드에서 입력 보정은 버그를 숨긴다.

## G-26. 탐지가 "낭비"라고 말하지 않아야 하는 경계 (2026-09-17)

**검증**: `DuplicateDetectorEdgeGoldenTest`

`detection` 분기 100% 기준을 채우며 드러난 경로들이다. 전부 **탐지하지 않는 쪽이 정답**인 케이스다 —
틀린 경고는 틀린 절감액보다 나쁘다. 사용자가 멀쩡한 구독을 해지하기 때문이다.

| | 입력 | 정답 |
|---|---|---|
| a | 혜택이 등급을 지정했고 사용자는 다른 등급 결제 중 | 겹침 **아님**. 같은 등급이면 겹침 |
| b | 사용자 등급을 모름 + 혜택이 등급 지정 | 겹침이라 **단정하지 않는다**. 혜택이 서비스 전체면 겹침 |
| c | 서비스가 다름 | 겹침 아님 |
| d | ~~FREE 가 아닌 혜택~~ | **2026-09-17 정정 — G-09 e·f 로 대체.** 아래 설명 참조 |
| e | 번들 구성 등급을 전부 결제 중이 아님 | 낭비 **아님**. 전부 결제 중이고 번들이 싸면 차액이 낭비 |
| f | 번들가 ≥ 개별 합계 | 낭비 아님. **같을 때도** 아님 |
| g | 등급을 모르는 구독 | 번들 판정에서 **제외**. 어느 등급인지 모르면 묶을 수 없다 |

e·f 는 `pricing` G-25 a·b 와 **같은 경계의 반대편**이다. 계산은 "번들을 쓸까"를 묻고 탐지는
"번들을 안 써서 손해인가"를 묻는데, 판단 기준은 같아야 한다. 한쪽만 고치면 두 화면이 다른 말을 한다.

**d 를 뒤집은 이유(2026-09-17).** 처음 이 케이스를 쓸 때 "FREE 가 아닌 혜택은 돈 내고 쓰는 것이니
겹침이 아니다"라고 적었는데, **현재 동작을 고정했을 뿐 옳은 규칙인지 확인하지 않았다.**
운영 데이터를 보니 그 판단이 틀렸다 — 할인 혜택을 안 쓰고 정가로 따로 내는 것도 낭비이고,
`BUNDLE_INCLUDED` 는 아예 "요금제에 포함"이라 전액이 낭비다. G-09 e·f 로 옮겨 다시 적었다.
**골든 케이스도 틀릴 수 있다. 현재 동작을 그대로 적은 케이스는 특히 그렇다.**

## G-27. 처리방침 버전이 오르면 (2026-09-17)

**검증**: `PrivacyApiTest`

`PrivacyPolicy.VERSION` 을 올리면 기존 회원의 `user_consent.policy_version` 이 뒤처진다.
**두 항목의 법적 근거가 달라 처리도 다르다** — 같이 처리하면 둘 중 하나가 반드시 틀린다.

| | 입력 | 정답 |
|---|---|---|
| a | 새로 가입한 회원 | 처음부터 `current: true` |
| b | 버전이 오른 뒤 구버전 기록 | `current: false`, **`agreed` 는 그대로**. 기록을 지우지 않는다 |
| c | `POST /me/consent/acknowledge` | ESSENTIAL 이 현재 버전·`current: true`. 서비스는 확인 전에도 막지 않는다 |
| d | c 이후 MARKETING | **`current: false` 그대로.** "방침을 읽었다"가 "광고를 받겠다"가 아니다 |
| e | d 이후 마케팅 재동의 | `current: true` |
| f | 비인증·CSRF 없이 acknowledge | **403**. 남이 대신 눌러 줄 수 있으면 기록이 의미가 없다 |

d 가 핵심이다. ESSENTIAL 은 계약 이행 근거라 **고지**이고, MARKETING 은 동의 근거라 **재동의**다.
하나의 버튼으로 둘 다 갱신하면 사용자가 동의한 적 없는 것에 동의한 것으로 기록된다.

**아직 없는 것**: 구버전 마케팅 동의로 발송하는 것을 막는 코드. 발송 기능 자체가 없어(D-21) 붙일
자리가 없다. 만들 때 `current == true` 검사를 그 지점에 넣어야 한다 — 문서에만 적힌 규칙은 지켜지지
않는다는 것을 G-15 b 에서 이미 겪었다.

---

## G-28. 가족결합 할인은 사용자가 말해 준 금액이다 (2026-09-17)

**검증**: `FamilyBundleDiscountGoldenTest` · `RecommendationApiTest`

카탈로그에 통신사별 결합 할인표가 없다. 그래서 **우리가 금액을 만들지 않고 사용자에게 받는다** —
해외 결제 구독에서 이미 쓰는 방법과 같다(D-17: 우리가 모르는 금액은 사용자가 확인한 값으로 채운다).
받은 금액은 `USER_PROVIDED` 이며 `DERIVED` 가 아니다. 우리가 계산한 값이 아니기 때문이다.

기준 금액은 기본료 55,000원이다.

| | 입력 | 정답 |
|---|---|---|
| a | 결합 O · 할인 11,000 · 3회선 | 55,000 − 11,000 = **44,000**. `CostLine` "가족결합 할인" `−11,000` · `USER_PROVIDED` |
| b | 결합 O · 할인액 없음 | **55,000 그대로.** 지어내지 않는다. `missingInputs` 에 할인액을 묻는 항목이 남는다 |
| c | 결합 X · 할인 11,000 | **55,000 그대로.** 결합 중이 아니라면 보낸 금액은 무시한다 |
| d | 결합 O · 할인 60,000 (기본료보다 큼) | **0원.** 음수 요금은 만들지 않는다. 남은 금액만큼만 깎는다 |
| e | 결합 O · 할인 11,000 · **선택약정 25% 동시** | **30,250**. 55,000 ×0.75 = 41,250 → −11,000 |
| f | 결합 O · 3회선만 · 할인액 없음 | **55,000 그대로.** 회선 수는 근거 문구에만 쓰고 금액에 넣지 않는다 |
| g | 할인액 음수 | **400** (`YGB-REQ-001`, field `familyBundleDiscountKrw`) |

**e 가 핵심이다.** 순서를 뒤집으면 값이 달라진다 — (55,000 − 11,000) × 0.75 = 33,000 이다.
`docs/domain.md §4` 가 선택약정 200 · 결합 300 으로 고정해 둔 이유가 이것이고, 이 케이스가 그 순서를 지킨다.

**d 를 반올림이 아니라 하한으로 적는 이유**: 결합 할인은 보통 여러 회선에 나눠 붙는데 사용자는 가구 전체
할인액을 적기 쉽다. 그 값이 내 요금보다 커도 입력을 틀렸다고 막지 않는다 — 0원까지만 깎고 결과는 낸다.

**회선 수를 왜 받고 금액에 안 쓰나**: 회선 수로 할인액을 역산하려면 통신사별 표가 필요한데 그게 없다.
받는 이유는 근거 문구("3회선 결합 기준")와, 표가 들어오는 날 사용자 입력과 대조할 값이 필요해서다.
없다고 적는 것도 정답이다 — 지금 금액에 쓰지 않는다는 뜻이다.

---

## G-29. 가족결합 할인은 지금 통신사에서만 유지된다 (2026-09-17)

**검증**: `RecommendationApiTest`

G-28 로 결합 할인을 받기 시작했더니 **그 금액을 모든 후보에서 똑같이 빼고 있었다.** 운영 화면에
"KCT(티플러스) 5G 티플 10GB — 가족결합 할인 −8,990원" 이 찍혔다. 알뜰폰에는 가족결합이 없고,
있더라도 통신사를 옮기는 순간 결합은 풀린다. **없어질 할인을 빼고 1등으로 올린 것**이라
사용자는 그 요금제로 갈아탄 뒤에 8,990원을 더 내게 된다 — 미사용 혜택은 0원(절대 원칙 1)의 위반이다.

결합 할인은 **통신사에 붙어 있다.** 지금 쓰는 통신사의 요금제로 바꾸면 유지되고, 다른 통신사로
옮기면 사라진다. 그래서 후보마다 "이 요금제가 지금 통신사인가"를 보고 적용 여부를 정한다.

현재 통신사는 `currentPlanId` 의 통신사 > `currentCarrier` 순으로 정한다. 요금제를 골랐다면 그 통신사가
확실하고, 이름만 적었다면 그 이름을 쓴다. 이름 비교는 `carrierExists` 와 같은 규칙이다(공백·대소문자 무시).

기준: 후보는 SKT 넷플플랜 55,000(넷플릭스 무료) · KT 웨이브플랜 45,000. 원하는 서비스는 넷플릭스,
할인액은 11,000원.

| | 입력 | 정답 |
|---|---|---|
| a | 현재 SKT · 결합 O · 11,000 | 넷플플랜 **44,000** (55,000 − 11,000) · 웨이브플랜 **58,500** (그대로) |
| b | 현재 KT · 결합 O · 11,000 | 웨이브플랜 **47,500** 이 1순위. 넷플플랜은 55,000 — **순위가 뒤집힌다** |
| c | 현재 통신사 없음(또는 "알뜰폰" 같은 분류) · 결합 O · 11,000 | 어디에도 적용하지 않는다(55,000 / 58,500) + 통신사를 묻는 안내 |
| d | 현재 `KT 엠모바일` vs 카탈로그 `KT엠모바일` | 같은 통신사로 본다 |
| e | a 처럼 적용됐고 다른 통신사 후보가 결과에 있음 | `missingInputs` 에 "다른 통신사로 옮기면 결합이 풀린다"는 안내 |
| f | `currentPlanId` 만 보냄(`currentCarrier` 없음) | 그 요금제의 통신사를 현재 통신사로 본다 — a 와 같은 결과 |

**b 가 핵심이다.** 결합 할인은 순위를 바꾼다. 모든 후보에서 똑같이 빼면 순위가 안 바뀌므로 버그가
보이지 않는다 — 한쪽에만 붙어야 이 케이스가 실패한다.

**c 를 "모르면 적용 안 함"으로 적는 이유**: 어느 통신사에 붙은 할인인지 모르면 어느 후보가 유지하는지도
모른다. 전부에 빼면 전부가 8,990원씩 싸 보이고, 그중 하나는 거짓이다. 지어내지 않는다(D-17).

## G-30. 현재 요금제를 알려주면 같은 기준으로 계산한다 (2026-09-17)

**검증**: `RecommendationApiTest`

지금까지 결과 표의 '현재' 열은 **사용자가 적어 준 통신비 + 구독 정가의 합**이었다. 화면의 메모였고
BE 는 그 값을 본 적이 없다. 그 옆의 '추천' 열은 할인을 반영한 실질월비용이라 **두 열의 기준이 달랐다** —
82,284원과 49,690원을 나란히 놓고 "32,594원 절약"으로 읽히지만 실제 절감액은 8,990원이었다.

`optional.currentPlanId` 로 지금 쓰는 요금제를 받으면, **후보와 똑같은 계산기·똑같은 컨텍스트**로
그 요금제의 실질월비용을 내어 `current` 에 싣는다. 같은 구독을 유지한다고 보고 계산하므로
비교 가능한 두 금액이 된다.

| | 입력 | 정답 |
|---|---|---|
| a | `currentPlanId` = 웨이브플랜(45,000) · 넷플릭스 희망 | `current.cost.monthlyTotal` = **58,500** (45,000 + 넷플릭스 13,500) |
| b | 같은 입력 | `current.monthlySavings` = 58,500 − 55,000 = **3,500** · `annualSavings` = **42,000** |
| c | `currentPlanId` 없음 | `current` = **null**. 화면은 예전처럼 사용자가 적은 값을 '현재' 열에 둔다 |
| d | `currentPlanId` 가 카탈로그에 없음 | **400 이 아니다.** `current` = null + 안내 한 줄. 모르면 막지 않는다(원칙 5-①) |
| e | 현재 요금제가 1순위보다 쌈 | `monthlySavings` 가 **음수 그대로**. 절감액이 없다는 사실을 감추지 않는다 |
| f | 현재 요금제 + 선택약정·결합 | 후보와 같은 컨텍스트로 계산한다. 결합 할인은 현재 요금제에 **반드시 적용된다**(그 통신사가 곧 현재 통신사, G-29 f) |

**요금제 이름은 프론트가 찾는다.** `GET /api/v1/catalog/plans` 가 이미 전량(1,707건)을 내려주고
디테일 화면이 통신사 목록을 만들려고 그것을 이미 부른다. 이름 검색 엔드포인트를 따로 만들지 않는 이유다 —
BE 가 받는 것은 사용자가 고른 **요금제 id** 하나다.


## G-31. 내레이터 응답 계약 (2026-09-17)

**검증**: `NarratorClientTest`

D-46 이 **운영에서 통째로 죽은 채 배포됐다.** 내레이터가 `notices` 를 보내기 시작했는데 BE 의
`requireObject(result, "message", "reasons")` 허용 목록에 그 이름이 없었다. 계약 밖 필드는 폐기하는
설계라(그 자체는 옳다) **설명·사유·안내가 전부 사라졌고**, 실패는 `Narration.none()` 으로 삼켜져
화면에는 그냥 아무 문장도 안 나왔다. 오류도 로그도 없다 — `NarratorClient` 에 테스트가 없었다.

| | 응답 | 정답 |
|---|---|---|
| a | `{message, reasons, notices}` | 셋 다 받는다. `notices` 가 화면 ⓘ 안내다 |
| b | `{message, reasons}` | 받는다. 안내가 없는 결과가 정상이다 |
| c | `{message, reasons, surprise}` | **폐기**(`Narration.none()`). 모르는 값을 화면에 흘리지 않는다 |
| d | 같은 응답 | 폐기한 **이유에 필드 이름이 적힌다**(`surprise`). 예외는 삼켜지므로 메시지가 유일한 흔적이다 |

**d 를 왜 테스트하나**: 이 예외는 `narrationFor` 이 삼킨다. 흡수는 옳지만 침묵은 아니다 —
이유를 적지 않으면 화면에서 문장이 사라진 것 말고는 흔적이 없고, 그게 오늘 세 번(허용 목록 누락 ·
서버 미배포 · 응답 500) 똑같이 "문장 없음" 으로만 보인 이유다. 이제 WARN 한 줄이 남는다.

**c 를 남겨 두는 이유**: 검사는 옳았다. 잘못된 것은 목록을 갱신하지 않은 쪽이다. 그래서 검사를 풀지 않고
목록만 고쳤고, 대신 **다음에 같은 일이 생기면 테스트가 먼저 빨개지도록** a 를 붙였다.


## G-32. 탐지 결과의 대상 이름과 내레이터 부재 (2026-09-18)

**검증**: `DetectionControllerTest`

`detection` 패키지는 분기 100% 가 강제되는데 D-46 이 `DetectionController.targetName` 과
`DetectionNarrator.fallback` 을 **테스트 없이** 넣어 `./gradlew check` 가 0.75 로 막혀 있었다.
막힌 것보다 나쁜 건 그 둘이 **사용자가 바로 보는 문자열**이라는 점이다 — 이름을 못 찾으면 화면에
`service:12` 같은 내부 참조가 그대로 찍히고, 내레이터가 없으면 탐지 화면이 통째로 빈다.

| | `targetRef` | 화면에 나가는 이름 |
|---|---|---|
| a | `service:1` (카탈로그에 있음) | **넷플릭스** |
| b | `service:99` (없음) | **서비스 #99** — 막지 않는다(D-17). 내부 참조를 그대로 내보내지 않는다 |
| c | `bundle:9` | **묶음 상품** |
| d | `promo:3` · `이름없음` · `null` | **그대로**(`promo:3` · `이름없음` · 빈 문자열). 지어내지 않는다 |

| | 내레이터 | 정답 |
|---|---|---|
| e | 있음 | 문구는 내레이터 것을 그대로 싣는다. 대상 이름은 **BE 가 찾아서 넘긴다**(내레이터는 카탈로그를 모른다) |
| f | 없음 | `fallback` — 규칙 이름과 `월 13,500원` 표기만. 문구는 비고 금액과 대상은 남는다 |
| g | 탐지 0건 | 빈 목록으로 200. "없음"도 결과다 |

**d 에서 카탈로그를 부르지 않는지도 본다.** `service:` 가 아닌 참조에 서비스 목록을 조회하면
탐지 한 건마다 카탈로그를 훑게 된다 — 이름을 못 찾을 게 뻔한 조회다.


## G-33. 세션은 디테일 입력 한 번보다 길어야 한다 (2026-09-18)

**검증**: `AuthSecurityTest` (`sessionIdleTimeoutAndAbsoluteLifetimeLimitCopiedCookies`)

사용자 보고: "로그인된 사람도 결과 볼 때 다시 로그인해야 한다." D-10 의 **15분 절대·5분 유휴**가 원인이었다.
디테일 모드는 회원 API 를 한 번도 부르지 않는다(카탈로그·추천은 공개, `credentials: omit`). 그래서 입력에 5분만
걸려도 `auth_session` 이 유휴로 죽고, 결과 화면의 `/me` 가 401 → 비회원용 게이트(D-36)를 회원이 본다.

| | 상태 | 정답 |
|---|---|---|
| a | JWT 만료 − 발급 | **24시간** (86,400,000ms). 쿠키 Max-Age 도 86,400 |
| b | 마지막 활동 110분 전 | `/me` **200**, `last_seen_at` 이 지금으로 갱신된다 |
| c | 마지막 활동 121분 전 | `/me` **401**, `/me/sessions` 목록에서도 사라진다 |

refresh 는 여전히 없다(D-48). 탈취 창이 길어지는 것은 DB 세션 행이 막는다 — 로그아웃·탈퇴·재발급이 지우고
`/me/sessions` 로 끊을 수 있다.

## G-34. 합본에 없는 요금제는 시작 시 물러난다 (2026-09-18)

**검증**: `MobilePlanSeedTest` (`bundledSeedRetiresPlansThatAreNoLongerInTheCsv`)

가상 사용자 3명으로 운영을 돌려 보니 **42세 SKT 사용자의 추천 1·2·4위가 존재하지 않는 요금제**였다 —
`KT 5G 웨이브팩 45,000원 · 999999MB`, `LGU+ 5G OTT택1`, `SKT 5G 넷플릭스팩`. 개발용 더미 id 1~5 다.
이름을 고치기 전의 `케이티엠모바일`·`케이티스카이라이프` 17행도 같이 살아 있었다(합계 22행).

`CatalogSeedLoader.run()` 은 업서트만 했다. CSV 에서 지우거나 이름을 바꾼 행은 DB 에서 **영원히 활성**이었다.
외부 스냅샷 경로(`loadSnapshot`)에는 물러나는 문장이 있었는데 운영은 그 경로가 아니다(`combined-csv` 미설정).

| | 상태 | 정답 |
|---|---|---|
| a | 합본에 없는 요금제 2건이 활성 | `run()` 뒤 둘 다 **`active=false`**. 지우지 않는다 — 회원의 현재 요금제일 수 있다(`findPlanById` 는 비활성도 찾는다) |
| b | 합본의 요금제 | 전부 활성. 활성 수 = 합본 행수 |
| c | 부분 파일(개발 더미)로 다시 적재 | **아무것도 물러나지 않는다.** 전체 파일일 때만 켠다 — 일부만 담은 파일에 켜면 나머지 전부가 죽는다 |

**c 가 있는 이유**: `DevSeedLoader` 가 같은 `loadMobilePlans` 로 더미 몇 줄을 싣는다. 플래그 없이 넣었으면
dev 프로파일에서 합본 1,700건이 전부 물러났을 것이다.


## G-35. 변경 시점은 이번 흐름에서 고른 요금제로 판정한다 (2026-09-18)

**검증**: `MeApiTest` (`switchTimingTakesCurrentPlanIdFromTheQueryFirst`)

캘린더의 `/me/switch-timing` 은 `app_user.current_plan_id` 만 읽었다. 디테일 1단계에서 요금제를 고른 사람도
마이페이지에 저장한 적이 없으면 400 이라, 브라우저 실측에서 호출이 **0회**였다(프론트가 조건을 열어도 실패 1회가 될 뿐).
프론트가 입력값을 프로필에 묵시적으로 저장하는 안(b)은 버리고, BE 가 `currentPlanId` 를 선택으로 받아 저장값보다
앞세운다(a). 흐름의 입력이 프로필을 덮어쓰지 않는다.

| | 입력 | 정답 |
|---|---|---|
| a | 저장값 없음 · `currentPlanId=1`(55,000) · 대상 2(45,000) | **200**, `currentMonthlyCost` 55,000 · 절감 10,000 |
| b | 저장값 3(99,000) · `currentPlanId=1` | 넘긴 값이 이긴다 — 55,000. **저장값은 그대로 3** |
| c | 저장값 3 · 파라미터 없음 | 예전대로 99,000 |
| d | 둘 다 없음 | 400 `currentPlan` (기존 `switchTimingRequiresCurrentPlanSet`) |
